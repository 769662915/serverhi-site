# 路由测试指南

## 🧪 测试清单

### 1. 本地开发测试

#### 启动开发服务器
```bash
npm run dev
```

#### 测试新增路由

**404 错误页面**
- [ ] 访问 `http://localhost:4321/404`
- [ ] 检查页面显示是否正常
- [ ] 测试所有导航链接是否可点击
- [ ] 验证响应式布局（手机、平板、桌面）
- [ ] 检查 Terminal 风格是否一致

**归档页面**
- [ ] 访问 `http://localhost:4321/archive`
- [ ] 验证文章按年月正确分组
- [ ] 检查文章计数是否准确
- [ ] 测试文章链接是否正确
- [ ] 验证时间倒序排列
- [ ] 检查响应式布局

**RSS 订阅源**
- [ ] 访问 `http://localhost:4321/rss.xml`
- [ ] 验证 XML 格式正确
- [ ] 检查所有文章是否包含
- [ ] 验证分类和标签信息
- [ ] 测试在 RSS 阅读器中打开

#### 测试优化的路由

**面包屑导航**
- [ ] 访问任意文章页面
- [ ] 检查面包屑是否显示
- [ ] 测试面包屑链接是否正确
- [ ] 验证当前页面高亮
- [ ] 检查响应式显示

**分类页面**
- [ ] 访问 `/category/docker`
- [ ] 检查面包屑导航
- [ ] 验证文章列表显示
- [ ] 测试空状态（如果适用）

**标签页面**
- [ ] 访问 `/tag/Docker`
- [ ] 检查面包屑导航
- [ ] 验证文章列表显示
- [ ] 测试标签计数

---

### 2. 构建测试

#### 执行构建
```bash
npm run build
```

#### 验证构建输出
```bash
# 检查构建是否成功
echo $?  # 应该返回 0

# 统计生成的页面数
find dist -name "*.html" | wc -l  # 应该是 35

# 检查关键文件
ls -la dist/404.html
ls -la dist/archive/index.html
ls -la dist/rss.xml
ls -la dist/sitemap-index.xml
```

#### 验证 Sitemap
```bash
# 查看 sitemap 内容
cat dist/sitemap-0.xml | grep -o "<loc>.*</loc>" | wc -l

# 验证不包含 404 和 search
cat dist/sitemap-0.xml | grep -E "(404|search)"  # 应该无输出
```

#### 验证 RSS Feed
```bash
# 检查 RSS 格式
xmllint --noout dist/rss.xml && echo "RSS 格式正确"

# 查看 RSS 内容
cat dist/rss.xml | head -50
```

---

### 3. 预览测试

#### 启动预览服务器
```bash
npm run preview
```

#### 测试所有路由

**主要页面**
- [ ] http://localhost:4321/
- [ ] http://localhost:4321/about
- [ ] http://localhost:4321/contact
- [ ] http://localhost:4321/search
- [ ] http://localhost:4321/archive ✨
- [ ] http://localhost:4321/404 ✨
- [ ] http://localhost:4321/privacy
- [ ] http://localhost:4321/terms

**内容页面**
- [ ] http://localhost:4321/posts
- [ ] http://localhost:4321/posts/example-docker-tutorial
- [ ] http://localhost:4321/posts/example-nginx-setup
- [ ] http://localhost:4321/posts/example-ubuntu-guide

**分类页面**
- [ ] http://localhost:4321/category/docker
- [ ] http://localhost:4321/category/linux
- [ ] http://localhost:4321/category/server-config
- [ ] http://localhost:4321/category/devops
- [ ] http://localhost:4321/category/security
- [ ] http://localhost:4321/category/troubleshooting

**标签页面**（测试几个）
- [ ] http://localhost:4321/tag/Docker
- [ ] http://localhost:4321/tag/Linux
- [ ] http://localhost:4321/tag/Nginx

**Feed 和 Sitemap**
- [ ] http://localhost:4321/rss.xml ✨
- [ ] http://localhost:4321/sitemap-index.xml
- [ ] http://localhost:4321/robots.txt

---

### 4. SEO 测试

#### Meta 标签检查
```bash
# 检查首页 meta 标签
curl -s http://localhost:4321/ | grep -E "<meta|<title"

# 检查文章页面 meta 标签
curl -s http://localhost:4321/posts/example-docker-tutorial/ | grep -E "<meta|<title"
```

#### Canonical URL 检查
```bash
# 检查 canonical 标签
curl -s http://localhost:4321/ | grep "canonical"
```

#### RSS 链接检查
```bash
# 检查 RSS 链接
curl -s http://localhost:4321/ | grep "rss"
```

#### Robots.txt 验证
```bash
# 查看 robots.txt
curl http://localhost:4321/robots.txt

# 验证内容
# - 应该允许所有爬虫
# - 应该包含 Sitemap 位置
# - 应该有针对主要搜索引擎的规则
```

---

### 5. 响应式测试

#### 使用浏览器开发工具

**移动端 (375px)**
- [ ] 导航菜单正常显示
- [ ] 面包屑响应式正常
- [ ] 文章卡片布局正确
- [ ] 404 页面布局正常
- [ ] 归档页面布局正常

**平板端 (768px)**
- [ ] 网格布局正确
- [ ] 导航显示完整
- [ ] 面包屑显示正常

**桌面端 (1280px+)**
- [ ] 所有元素正确对齐
- [ ] 最大宽度限制生效
- [ ] 间距和边距正确

---

### 6. 性能测试

#### Lighthouse 测试
```bash
# 安装 lighthouse
npm install -g lighthouse

# 运行测试
lighthouse http://localhost:4321/ --view

# 检查指标
# - Performance: > 90
# - Accessibility: > 90
# - Best Practices: > 90
# - SEO: > 90
```

#### 页面加载测试
- [ ] 首页加载时间 < 2s
- [ ] 文章页面加载时间 < 2s
- [ ] 分类页面加载时间 < 2s
- [ ] 归档页面加载时间 < 2s

---

### 7. 链接完整性测试

#### 检查内部链接
```bash
# 使用 linkchecker 或类似工具
npm install -g broken-link-checker

# 检查所有链接
blc http://localhost:4321/ -ro
```

#### 手动检查关键链接

**Header 导航**
- [ ] Home → /
- [ ] Tutorials → /posts
- [ ] Docker → /category/docker
- [ ] Linux → /category/linux
- [ ] DevOps → /category/devops
- [ ] Search → /search

**Footer 链接**
- [ ] All Tutorials → /posts
- [ ] Archive → /archive ✨
- [ ] Search → /search
- [ ] About → /about
- [ ] Contact → /contact
- [ ] Privacy → /privacy
- [ ] Terms → /terms

**面包屑链接**
- [ ] 文章页面面包屑
- [ ] 分类页面面包屑
- [ ] 标签页面面包屑

---

### 8. 功能测试

#### 搜索功能
- [ ] 搜索框显示正常
- [ ] 输入关键词返回结果
- [ ] 搜索结果链接正确
- [ ] 空搜索提示正确

#### 主题切换
- [ ] 亮色主题显示正常
- [ ] 暗黑主题显示正常
- [ ] 主题切换动画流畅
- [ ] 主题偏好保存正确

#### 文章功能
- [ ] 相关文章推荐正确
- [ ] 标签链接正确
- [ ] 分类链接正确
- [ ] 代码高亮正常
- [ ] 图片加载正常

---

### 9. 错误处理测试

#### 404 页面测试
```bash
# 访问不存在的页面
curl -I http://localhost:4321/non-existent-page

# 应该返回 404 状态码
# 应该显示自定义 404 页面
```

#### 测试场景
- [ ] 访问不存在的文章
- [ ] 访问不存在的分类
- [ ] 访问不存在的标签
- [ ] 访问错误的 URL 格式

---

### 10. 浏览器兼容性测试

#### 测试浏览器
- [ ] Chrome (最新版)
- [ ] Firefox (最新版)
- [ ] Safari (最新版)
- [ ] Edge (最新版)
- [ ] Mobile Safari (iOS)
- [ ] Chrome Mobile (Android)

#### 测试内容
- [ ] 页面渲染正常
- [ ] CSS 样式正确
- [ ] JavaScript 功能正常
- [ ] 动画效果流畅
- [ ] 字体加载正确

---

## 🐛 常见问题排查

### 构建失败
```bash
# 清理缓存
rm -rf node_modules/.astro
rm -rf dist

# 重新安装依赖
npm install

# 重新构建
npm run build
```

### RSS Feed 不显示
```bash
# 检查 RSS 文件是否生成
ls -la dist/rss.xml

# 检查 RSS 格式
xmllint --noout dist/rss.xml

# 检查 SEO 组件中的 RSS 链接
grep "rss" src/components/SEO.astro
```

### 面包屑不显示
```bash
# 检查 Breadcrumb 组件
cat src/components/Breadcrumb.astro

# 检查页面是否导入组件
grep "Breadcrumb" src/pages/posts/[slug].astro
```

### 404 页面不工作
```bash
# 检查 404.astro 文件
ls -la src/pages/404.astro

# 检查构建输出
ls -la dist/404.html

# 在预览模式下测试
npm run preview
# 访问不存在的页面
```

---

## ✅ 测试通过标准

### 功能测试
- ✅ 所有路由可访问
- ✅ 所有链接正常工作
- ✅ 搜索功能正常
- ✅ 主题切换正常
- ✅ 面包屑导航正确

### SEO 测试
- ✅ Sitemap 生成正确
- ✅ RSS Feed 格式正确
- ✅ Meta 标签完整
- ✅ Robots.txt 配置正确
- ✅ Canonical URLs 正确

### 性能测试
- ✅ Lighthouse 分数 > 90
- ✅ 页面加载时间 < 2s
- ✅ 构建时间 < 1s
- ✅ 无构建错误

### 响应式测试
- ✅ 移动端布局正常
- ✅ 平板端布局正常
- ✅ 桌面端布局正常
- ✅ 所有断点正确

---

## 📝 测试报告模板

```markdown
# 路由测试报告

**测试日期**: YYYY-MM-DD
**测试人员**: [姓名]
**测试环境**: [开发/预览/生产]

## 测试结果

### 新增路由
- [ ] 404 页面: ✅/❌
- [ ] 归档页面: ✅/❌
- [ ] RSS Feed: ✅/❌

### 优化路由
- [ ] 文章详情页: ✅/❌
- [ ] 分类页面: ✅/❌
- [ ] 标签页面: ✅/❌

### SEO 测试
- [ ] Sitemap: ✅/❌
- [ ] Robots.txt: ✅/❌
- [ ] Meta 标签: ✅/❌

### 性能测试
- [ ] Lighthouse 分数: [分数]
- [ ] 页面加载时间: [时间]
- [ ] 构建时间: [时间]

## 发现的问题
1. [问题描述]
2. [问题描述]

## 建议
1. [建议内容]
2. [建议内容]

## 总体评价
[通过/需要修复]
```

---

**最后更新**: 2026-02-07
**版本**: 1.0.0
